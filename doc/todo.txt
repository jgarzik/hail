
general:
	- support multiple threads handling client activity
	- global thread pool, shared across volumes
	- optional private thread pool for a single volume
	- hook into a username/password database

storage backends:
	- support for "one big file" (or blk device) storage backend

test suite:
	- fine-grained HTTP header tests
		- if-*
		- date missing
		- etc.

network:
	- BUG: make MY_ENDPOINT a cfgfile option, not compile-time option
	- BUG: large-objects over SSL fails.  SSL_read() returns
	  SSL_ERROR_WANT_READ, but epoll never delivers another event.


